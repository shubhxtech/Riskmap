# ============================================================
#  RiskMap — Windows Requirements
#  Install in a conda env (Python 3.10 recommended):
#
#    conda create -n riskmap python=3.10
#    conda activate riskmap
#    pip install -r requirements_windows.txt
# ============================================================

# ── GUI ─────────────────────────────────────────────────────
PyQt5==5.15.9
PyQtWebEngine==5.15.6
qtawesome==1.3.1

# ── Computer Vision ─────────────────────────────────────────
opencv-python==4.8.1.78
Pillow>=9.5.0

# ── Deep Learning ───────────────────────────────────────────
# PyTorch: install from pytorch.org for your CUDA version,
# e.g. for CUDA 11.8:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
# CPU-only fallback (slower, but works everywhere):
torch
torchvision

# TensorFlow  (Faster R-CNN detector via TF Hub)
# Windows: TF 2.10 is the last version with native GPU support
tensorflow==2.10.1
tensorflow-hub

# HuggingFace (BEiT classifier)
transformers>=4.35.0
tokenizers>=0.14.0
huggingface_hub>=0.19.0

# ── Geospatial & Mapping ─────────────────────────────────────
folium>=0.14.0
branca>=0.6.0

# ── Data / ML ────────────────────────────────────────────────
numpy>=1.24.3,<2.0
scikit-learn>=1.3.0
pandas>=2.0.0
matplotlib>=3.7.0

# ── Utilities ────────────────────────────────────────────────
requests>=2.31.0
python-dotenv>=1.0.0
pytz>=2023.3
tqdm>=4.65.0
tenacity>=8.2.0
configparser>=5.3.0
babel>=2.12.0

# ── Build tool ───────────────────────────────────────────────
pyinstaller>=6.3.0
