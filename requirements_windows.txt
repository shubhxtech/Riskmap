# ============================================================
#  RiskMap — Windows Requirements
#
#  QUICK START:
#    conda create -n riskmap python=3.10 -y
#    conda activate riskmap
#    pip install -r requirements_windows.txt
#
#  FOR GPU ACCELERATION (recommended):
#    After pip install, run:
#      conda install -c conda-forge cudatoolkit=11.2 cudnn=8.1.0 -y
#    Then restart the app. TensorFlow will auto-detect the GPU.
#    PyTorch CUDA: replace the torch/torchvision lines below with:
#      pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
# ============================================================

# ── GUI ─────────────────────────────────────────────────────
PyQt5==5.15.9
PyQtWebEngine==5.15.6
qtawesome==1.3.1

# ── Computer Vision ─────────────────────────────────────────
opencv-python==4.8.1.78
Pillow>=9.5.0

# ── Deep Learning — PyTorch ─────────────────────────────────
# CPU-only (works everywhere, slower):
torch
torchvision
# GPU (CUDA 11.8) — uncomment and comment out the 2 lines above:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118

# ── Deep Learning — TensorFlow ──────────────────────────────
# TF 2.10 = last version with native Windows GPU support.
# GPU requires: conda install -c conda-forge cudatoolkit=11.2 cudnn=8.1.0
tensorflow==2.10.1
tensorflow-hub>=0.12.0

# ── HuggingFace (BEiT classifier) ───────────────────────────
transformers>=4.35.0
tokenizers>=0.14.0
huggingface_hub>=0.19.0

# ── Geospatial & Mapping ─────────────────────────────────────
folium>=0.14.0
branca>=0.6.0

# ── Data / ML ────────────────────────────────────────────────
numpy>=1.24.3,<2.0
scipy>=1.10.0
scikit-learn>=1.3.0
pandas>=2.0.0
matplotlib>=3.7.0
cartopy

# ── Utilities ────────────────────────────────────────────────
requests>=2.31.0
python-dotenv>=1.0.0
pytz>=2023.3
tqdm>=4.65.0
tenacity>=8.2.0
configparser>=5.3.0
babel>=2.12.0

# ── Build tool ───────────────────────────────────────────────
pyinstaller>=6.3.0
